recipe({
  libraries: [
    "fettuccine.alfredo"
  ],
  scripts: [
    // write script URL
  ]
}).then(function(){
  //write page specific logic
  alert(salt && milk && butter && parmigianoReggiano && fettuccine && 'Digin!!');
});
